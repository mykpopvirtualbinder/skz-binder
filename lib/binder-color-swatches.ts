/**
 * Paleta de acento para binders / separadores: valores por tema en `globals.css`
 * (`--binder-palette-1` … `--binder-palette-8`).
 */
export const BINDER_ACCENT_SWATCHES = [
  "var(--binder-palette-1)",
  "var(--binder-palette-2)",
  "var(--binder-palette-3)",
  "var(--binder-palette-4)",
  "var(--binder-palette-5)",
  "var(--binder-palette-6)",
  "var(--binder-palette-7)",
  "var(--binder-palette-8)",
] as const;
